<div class="container px-0 my-3">

	<div class="card rounded-3 border-0">
		<div
			class="card-body d-flex align-items-center justify-content-between">

			<!-- Photo + titre -->
			<div class="d-flex align-items-center gap-3">

				<img src="{{ asset('assets/images/profile/profil-avatar-homme.jpg') }}" alt="Photo de profil" class="rounded-circle img-thumbnail" width="100" height="100" id="profilePhoto">

				<div>
					<h5 class="fw-bold mb-1">Photo de profil</h5>
					<p class="text-muted small mb-0">Formats acceptés : JPG, PNG, WEBP</p>
				</div>

			</div>

			<!-- Bouton -->
			<div>
				<button class="btn btn-link d-flex align-items-center gap-2" id="btnChangePhoto">
					<i class="bi bi-pencil-square"></i>
					Modifier la photo de profile
				</button>
				<input type="file" id="photoInput" class="d-none" accept="image/*">
			</div>

		</div>
	</div>

</div>

<div class="alert alert-success d-none mt-3 shadow-sm" id="alertSuccess" role="alert">
	La photo a bien été mise à jour !
</div>





 <script>
const defaultProfileUrl = "";
const profilePhoto = document.getElementById('profilePhoto');
const photoInput = document.getElementById('photoInput');
const btnChangePhoto = document.getElementById('btnChangePhoto');
const alertSuccess = document.getElementById('alertSuccess');



// 🔹 Clique sur "Modifier la photo" => ouvre le sélecteur
btnChangePhoto.addEventListener('click', () => {
  photoInput.click();
});

// 🔹 Quand un fichier est choisi
photoInput.addEventListener('change', async () => {
  const file = photoInput.files[0];
  if (!file) return;

  const formData = new FormData();
  formData.append('imageFile', file);

  try {
    // 👉 Il manquait le mot-clé "await" ici 👇
    const response = await fetch("/api/profile/photo", {
      method: 'POST',
      body: formData,
      credentials: 'include'
    });

    const data = await response.json();

    if (data.error) {
      alert('❌ ' + data.error);
      return;
    }

    console.log('✅ Réponse serveur :', data);

    // 🔹 On remplace l'image par celle renvoyée par l'API
    if (data.imageUrl) {
      profilePhoto.src = data.imageUrl + '?t=' + new Date().getTime(); // éviter le cache navigateur
getImageProfile()
alertSuccess.classList.remove('d-none')


setInterval(() => {
alertSuccess.classList.add('d-none')

}, 2000);


	} else if (data.imageName) {
      profilePhoto.src = '/uploads/avatars/' + data.imageName + '?t=' + new Date().getTime();
    } else {
      // si aucun nom renvoyé, revenir à la photo par défaut
      profilePhoto.src = defaultProfileUrl;
    }

  } catch (err) {
    console.error('Erreur upload :', err);
    alert('Erreur lors du téléversement : ' + err.message);
  }
});
async function getImageProfile(){
const profilePhoto = document.getElementById('profilePhoto');



const response = await fetch("/api/profile/image");
    const data = await response.json();
    if (data.error) {
      alert('❌ ' + data.error);
      return;
    }

    console.log('✅ Réponse serveur :', data);
profilePhoto.src = '/uploads/avatars/' + data.imageProfile;
}
getImageProfile()

</script>
